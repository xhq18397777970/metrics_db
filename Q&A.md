问题：1、存储的逻辑是怎么样，一个集群的一个指标存储为一条数据，即列名为集群、指标、值、标签、资源名称。为什么不设计为一条数据直接包含一个集群所有的指标，此时列名为集群、cpu、qps、http_code_2xx.....。为什么不是宽表存储
答：


2、bucket time 和 collected At 时间是怎么样，如果要进行基线计算，如何计算。（如：计算最近 2 小时，集群 xx 的 cpu 基线（平均值）

3、自动采集的时间点，一直持续跑吗？

4、是否可以查看当前正在进行的任务

5、进行一个时间段的指标查询（即对该 5 分钟间隔，所有集群的所有指标查询到入库，耗时多久）
